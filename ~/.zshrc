# Source Node Version Manager
if [[ -s ~/.nvm/nvm.sh ]]; then
    source ~/.nvm/nvm.sh
fi

# Source Node Version Manager auto complete
if [[ -s ~/.nvm/bash_completion ]]; then
    source ~/.nvm/bash_completion
fi

# Source Rust
source ~/.cargo/env
